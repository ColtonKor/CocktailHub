<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cocktail Hub - Auth</title>
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar">
        <ul class="nav-list">
            <li><a href="/" class="nav-link">Home</a></li>
            <li><a href="/posts" class="nav-link">Posts</a></li>
            <li><a href="/auth" class="nav-link nav-selected">Login</a></li>
        </ul>
    </nav>

    <div class="container">
        <h1>Authentication</h1>
        <form action="/auth/toggle" method="POST">
            <button class="button">
                <%= user ? 'Logout' : 'Login as username1' %>
            </button>
        </form>
        
        <% if (user) { %>
            <div class="user-info">
                <p>Currently logged in as: <%= user.username %></p>
            </div>
        <% } %>
    </div>
</body>
</html>